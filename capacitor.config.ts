import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'Ionic-Anglart-Coure',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
